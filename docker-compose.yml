services:
postgres:
image: postgres:16-alpine
container_name: atomassist-postgres
environment:
POSTGRES_DB: atomassist
POSTGRES_USER: atomassist
POSTGRES_PASSWORD: atomassist
ports:
- "5432:5432"
volumes:
- atomassist_postgres_data:/var/lib/postgresql/data

volumes:
atomassist_postgres_data:
